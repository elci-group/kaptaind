[package]
name = "kaptaind-diff"
version = "9.6.3"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
toml = "0.8"
semver = "1"

[dev-dependencies]
tempfile = "3"
