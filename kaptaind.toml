# kaptaind configuration — auto-generated by `kaptaind-cli init`

[watch]
path = "."
recursive = true
ignore_file = ".kaptainignore"

[cluster]
window = 5

[weights]
s = 0.35
a = 0.30
d = 0.20
r = 0.15

[push]
enabled = false
branch = "main"

[ratelimit]
min_commit_interval = 10

[test]
command = "cargo test"
required = true

# [staging]
# mode = "all"        # "all" (default), "cluster" (only changed files), or "pattern"
# include = ["src/**"] # only used in "pattern" mode
# exclude = ["*.log", ".env*"]

# [inference]
# enabled = true
# provider = "kimi"     # "auto", "kimi", "anthropic", "openai", or "ollama"
# model = "kimi-k2.5"
# timeout_secs = 30

# [inference.kimi]
# endpoint = "coding"   # "global", "china", or "coding"
# model = "kimi-for-coding"
# thinking = false
# extended_context = false
